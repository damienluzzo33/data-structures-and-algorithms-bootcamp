//?  MULTIPLE POINTERS

//*  Write a function called averagePair that accepts a sorted array of integers and a target average, determine if there is a pair of values in the array where the average of the pair equals the target average. There may be more than one pair that matches the average target. If the average matches the target val return true, or try returning the total number of pairs as a bonus.

//* EXAMPLES:
//* [1, 3, 5, 7] , 6  -->  true
//* [2, 4, 6, 8] , 6  -->  true
//* [2, 5, 6, 8] , 6  -->  false

function averagePair(arr, avg) {
    // TODO: Define your variable pointers to start at the beginning and the end of the array
    
    // TODO: while the starting pointer is less than the end pointer (to prevent crossing)
    while () {

        // TODO: if the average of the values currently pointed at equals the average return true
        if () {
            
        // TODO: else, id the average is less than the value, increment the starting pointer
        } else if () {
            
        // TODO: otherwise, decrement the end pointer
        } else {
            
        }
    }
    // TODO: if we break out of the loop, what do we return here?
    
}
