//?  MULTIPLE POINTERS

//*  Implement a function called countUniqueValues which accepts a sorted array and counts the unique values in the array. There can be negative numbers in the array, but it will always be sorted.

function countUniqueValues(arr) {
    // TODO: take care of edge case
    if() {
        
    }
    // TODO: initialize our variable pointers
    
    // TODO: while the second pointer is less than the length of the array
    while() {

        // TODO: if the value at the first pointer isn't equal to the value at the second pointer
        if() {

            // TODO: increment the first pointer, move the second value into the unique subarray, and then increment the second pointer
            
        // TODO: else, increment the second pointer
        } else {
            
        }
    }
    // TODO: return the length of the subarray (which should be the position of pointer one + 1)
    
}
